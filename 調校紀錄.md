# 採集效率調校紀錄

目標：最大化**每小時確認狩獵數**。

比較任何兩次執行之前，先扣掉遊戲強制的等待（`target_too_strong` 與
`hunt_capacity_full` 各五分鐘）。隊伍受傷時遊戲本來就拒絕狩獵，那段時間買的是恢復，
不是可以省下來的成本 —— 沒扣掉就會把「等得比較久」誤讀成「跑得比較慢」。

所有數字取自 2026-07-26 凌晨的實機執行，資料來源是 `logs/events-YYYYMMDD.jsonl`。

## 時間都花在哪裡

一次狩獵的動作本身很快，慢的是它周圍的感知循環：

| 項目 | 中位數 |
|---|---|
| 點恐龍 → 確認狩獵成功（n=47） | **3.3 秒**（最快 3.0，最慢 11.3） |
| 每次狩獵消耗的規劃循環 | **6 個** |
| 一個規劃循環：擷取 | 278 ms |
| 一個規劃循環：**完整辨識** | **2634 ms** |
| 驗證用的針對性辨識（只掃預期的下一個 UI） | **380 ms** |

`6 個循環 × 約 2.9 秒 ≈ 17 秒`，與實測的每小時約 220 次（每次約 16 秒）吻合。

**動作只佔約 20%，其餘八成是在等辨識。**

規劃循環的階段分布（22.6 分鐘、283 個循環、47 次狩獵）：

| 階段 | 循環數 | 佔比 |
|---|---|---|
| `hunt_control`（點狩獵／確認鍵） | 105 | 37.0% |
| `hunting`（選恐龍） | 78 | 27.5% |
| `map_settle`（等畫面穩定） | 65 | 22.9% |
| `recenter`（回中） | 28 | 9.9% |
| 其他（中斷彈窗、冷卻） | 8 | 2.8% |

## 最大的槓桿：規劃階段的辨識成本

**同一份素材，驗證只要 380 ms，規劃要 2634 ms —— 差 7 倍。**

差別在於驗證只掃「預期的下一個 UI」（`verify.success_transitions`），規劃每次都跑完
全部 20 種素材。既然 380 ms 的針對性掃描已經在線上穩定運作，這個差距就不是理論值，
而是已經被證明可行的空間。

規劃階段確實需要比驗證更寬的視野（它要能發現中斷彈窗、信箱、回中按鈕），但不需要
每一幀都掃全部二十種。

### 這 1741 ms 花在哪（2026-07-26 實測，900x1600 實機畫面，機器空閒）

| 素材 | ms | 佔比 |
|---|---|---|
| `device_history_confirm_button` | **370** | **21%** |
| `hunt_button` | 103 | 6% |
| 其餘 15 個素材 | 各 65–99 | 合計 63% |
| `HuntCapacityDetector` | 90 | 5% |
| 其餘四個版面偵測器 | 各 &lt;1 | ~0% |

`device_history_confirm_button` 一個就吃掉五分之一——它配了四個 `scales`
（`[0.88, 0.9, 0.95, 1.0]`），而它是**只有在新裝置登入時才會出現的對話框**。

值得注意的是 **matchTemplate 的成本與素材大小幾乎無關**（18x18 的恐龍標籤 84 ms，
505x138 的重複狩獵警告 72 ms），也**與畫面上有幾個命中無關**（同一份掃描，找到 5 個
或 50 個都是同樣的毫秒數）。成本只跟「掃幾個素材、每個幾個 scale」成正比。所以能省的
唯一方式就是**少掃**。

### 已實作：依階段限制素材集合（`planner.stage_scoped_scan`）

planner 在每個 cycle 前宣告目前階段能夠採取行動的偵測種類，engine 改呼叫
`detect_types()`：

| 階段 | ms | 省下 |
|---|---|---|
| 全掃描（原本每個 cycle） | 1741 | — |
| 地圖／狩獵 | 1010 | 42.0% |
| 信箱流程 | 770 | 55.8% |
| 信箱滿恢復 | 1036 | 40.5% |

拿掉的是三個登入／開場對話框（508 ms）與不在信箱流程時的三顆信箱按鈕。**沒有**拿掉
狩獵視窗的按鈕與中央蛋——中央蛋一旦不掃，錨點就從實測退回推算，而那正是
`anchor_window` 餓死候選的根源（見下）。

安全機制有三層：連續兩個 cycle 規劃不出目標就把下一次放回全掃描（窄掃描漏看的東西
長得跟空地圖一樣，所以空轉就是察覺的訊號）；最長 30 秒一定全掃一次；遊戲重啟後強制
全掃，因為重啟會走回那些開場對話框。以實測的 43% 空轉率估算，平均每 cycle
**1153 ms，比現行少 33.8%**。

### 已實作：半解析度比對（`assets/manifest.json` 的 `match_scale`）

matchTemplate 有兩個實測性質，決定了唯一能省的方式：

- **成本與素材大小無關**：18×18 的恐龍標籤 84 ms，505×138 的重複狩獵警告 72 ms。
- **成本與命中數無關**：同一份掃描，找到 5 個或 50 個都是同樣的毫秒數。

成本只跟「搜尋範圍的像素數 × 掃幾個素材 × 每個幾個 scale」成正比。所以把搜尋範圍砍半
（900×1600 → 450×800）是接近四倍的加速，而縮圖本身只要 0.3 ms。

信心值不但沒掉，多半還微升——`INTER_AREA` 縮圖等於做了一次低通濾波，把干擾比對的高頻
雜訊去掉了。合成測試 17 個素材全數通過、位置誤差 0 px，餘裕最小的是
`hunt_max_group_button` 的 +0.043。

| 掃描類型 | 全解析度 | 混合解析度 | 加速 |
|---|---|---|---|
| 全掃描（17 種） | 1667 ms | **492 ms** | 3.4× |
| 地圖窄掃描（11 種） | 894 ms | 286 ms | 3.1× |
| 驗證掃描（3 種） | 252 ms | 62 ms | 4.1× |

實機畫面對照：恐龍 10 個對 10 個、`own_hunt_path` 91 對 91、座標 **0 px 差異**。

**兩個例外要記住：**

`dinosaur` 維持 `match_scale: 1.0`。18×18 砍半成 9×9 後，NMS 之後從 10 個變 15 個——多
出來的 5 個在全解析度只有 0.536–0.581 的邊緣匹配，真假無法離線判定。留全解析度只多花
79 ms，換掉整個不確定性。

`map_center_egg` 反而變得**更容易被偵測到**：同一張畫面，全解析度 0.716（低於 0.75 門檻、
看不到），半解析度 0.783（看得到），兩者落在同一個位置。這可能比速度更值錢——見下面
「錨點一旦改成推算」那節：中央蛋可見率只有 12–24%，而錨點從實測退化成推算時，
`anchor_window` 每個 cycle 的濾除數從 0.05 跳到 4。

### 還沒做的

- **抓圖**：實測 265 ms × 14,092 次 = 62.2 分鐘，佔牆鐘 24%，兩條開發線都沒碰過它。
  半解析度做完之後它會變成最大的一塊（約佔新預算的 37%）。可能的方向：`screencap` 不加
  `-p` 拿 RGBA 原始緩衝，省掉裝置端 PNG 編碼與 `cv2.imdecode`，代價是傳輸量從約 500 KB
  變成 5.76 MB。**未量測。**
- 限制搜尋 ROI（信箱、回中、離巢按鈕的位置是固定的）
- `device_history_confirm_button` 的四個 scales 是否真的都需要

辨識成本每降一半，每小時次數大約翻倍。這是目前唯一有這種量級的槓桿。

## 已驗證無效的調整

### 轉場逾時（`speed_profiles.fast`）

把 `dinosaur_delay_ms` 300→1800、`hunt_button_delay_ms` 900→2500、
`hunt_confirm_delay_ms` 1200→3500：

| 設定 | 分鐘 | 狩獵 | 補點率 | 扣掉冷卻後每小時 |
|---|---|---|---|---|
| 300 / 900 / 1200 | 12.9 | 47 | 3.4% | 218.5 |
| 1800 / 2500 / 3500 | 22.6 | 47 | 3.8% | 約 224 |

**沒有改善。**失敗的點擊是 ADB tap 真的沒送進去（重新規劃時同一顆按鈕仍以相同信心值
留在原位），不是驗證太早宣告失敗，所以拉長等待救不了，只讓每次失敗多跑一兩次辨識。

注意 `驗證失敗率` 這個欄位**不能跨設定比較**：它算的是輪詢次數而非動作次數，逾時
拉長會多輪詢，分子分母一起變大。要看效果請看**補點率**（真正需要再點一次的比例）。

## 不該調的

| 參數 | 現值 | 原因 |
|---|---|---|
| `planner.action_cooldowns_ms.target_too_strong` | 300000 | 隊伍受傷時遊戲拒絕狩獵，等待買的是恢復時間 |
| `planner.action_cooldowns_ms.no_available_dinosaurs` | 90000 | 同上，隊伍全部派出時無事可做 |
| `planner.capacity_wait_seconds` | 300 | 同上 |

診斷包 `summary.json` 的 `optimization` 區塊已排除 `action_cooldown`，不會把這段時間
報成可調參數。

另外，**依等級篩選目標沒有意義**：能否打贏取決於當下可用隊伍的數值，同一隻目標在隊伍
恢復後就打得過。打不過就等，不需要預測。

## 錨點一旦改成推算，`anchor_window` 就開始餓死候選

2026-07-26 的 0.2.15 三小時執行（735 次狩獵）拆開來看，**每次狩獵有 7.6 個候選被
`anchor_window` 這條規則濾掉**，而且濾掉的數量隨著離上次回中愈遠而暴增：

| 距上次回中的狩獵數 | cycles | 空轉率 | `anchor_window` 濾掉數/cycle |
|---|---|---|---|
| 0（剛回中） | 389 | 42.4% | **0.05** |
| 1 | 165 | 23.0% | 3.56 |
| 3 | 190 | 61.6% | 3.93 |
| 4 | 148 | 65.5% | 4.31 |
| 7 | 28 | 82.1% | 3.11 |

原因在 `planning.py` 的 `_reject_dinosaur`：這條規則要求候選在「地圖回正後仍留在畫面
上」，判斷依據是 `_last_anchor`。中央蛋看得見時 `_last_anchor` 是**實測**的，此時幾乎
不誤殺（0.05）；點下恐龍之後地圖平移、蛋離開畫面，錨點改成**推算**（假設地圖以被點的
恐龍為中心），誤差每次狩獵累積一次，一次狩獵後就跳到 3.56。

**有效工作窗口只有回中後約兩次狩獵**，其餘時間有六成以上的 cycle 找不到目標。

這也是為什麼縮小掃描集合時**不能**把 `map_center_egg` 拿掉：省下 77 ms，換來錨點提早
退化成推算，得不償失。

## 待驗證（依預期效益排序）

0. **主機負載** —— 同一份工作在不同背景負載下，辨識 p50 實測是 2397 ms 對 3668 ms
   （+53%），而 `detection.py`、`capture.py`、`manifest.json` 逐位元組相同。每次狩獵
   17.9 秒裡有 9.8 秒在辨識，所以背景多開一個遊戲就吃掉約 15–20% 產量。**比較任何兩次
   執行之前，先確認背景負載相近**，否則參數的效果會被這一項蓋過去。
1. **降低規劃階段的辨識成本** —— 唯一有倍數級效益的方向，見上。
2. **`planner.map_settle_frames` / `map_settle_max_frames`**（現值 2 / 12）——
   佔 22.9% 的規劃循環，約每次狩獵 1.4 個循環、4 秒。要先確認縮短後不會讓
   地圖位移預測失準。
3. **`planner.recenter_every`**（現值 10）—— 佔 9.9%，每 10 次狩獵約 6–8 秒。
   放寬會累積地圖漂移，需要先量測漂移速度。
4. **`planner.mail_after_hunts`**（現值 30）—— 一次信箱流程約 20 秒，佔比很小，
   優先度最低。

## 量測方式

```bash
# 診斷包內建，不需另外工具
python3 -c "
from pathlib import Path
from dino_bot.optimization import summarize_events
import json
lines = Path('logs/events-YYYYMMDD.jsonl').read_text().splitlines()
print(json.dumps(summarize_events(lines), ensure_ascii=False, indent=2))"
```

`stage_cycles` 給階段分布，`detect_ms` 給辨識成本，`hunts_per_hour` 給結果。
比較兩次執行時記得先用時間窗切開，並確認兩邊的冷卻次數相近。
