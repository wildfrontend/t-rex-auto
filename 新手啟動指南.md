# 猛龍計畫新手啟動指南

從零開始,到 Bot 自動掛機。全程約 15–30 分鐘(視下載速度)。

---

## 第一步:安裝模擬器(BlueStacks)

Bot 透過 ADB 操作 Android 模擬器裡的 Dino Mutant。

### Windows

1. 到 [bluestacks.com](https://www.bluestacks.com/) 下載 **BlueStacks 5**,安裝並啟動。
2. 在模擬器裡開啟 Google Play,登入帳號,搜尋並安裝 **Dino Mutant**。
3. 調整模擬器設定(右側齒輪 →「設定」):
   - **螢幕**:直式(Portrait),解析度 **1080 × 1920**,DPI **320**
     (Bot 的畫面辨識依賴這個解析度,務必一致)
   - **進階**:勾選 **Android 調試橋(ADB)**,並**記下顯示的埠號**
     (例如 `127.0.0.1:5555`;多實例管理器建立的實例常是 `16384` 起跳)
   - **效能**:CPU 4 核心、記憶體 4GB 以上較穩
4. 重啟模擬器讓設定生效。

### macOS(Apple Silicon)

1. 下載 **BlueStacks Air**,安裝並啟動。
2. 同樣安裝 Dino Mutant、設定直式 1080 × 1920。
3. 開啟 ADB(設定 → 進階);macOS 上埠號通常是 `127.0.0.1:5555`。

> 💡 已知狀況:BlueStacks Air 偶爾會自行關閉或掉 ADB 連線,重開 BlueStacks 即可恢復。

---

## 第二步:安裝 Bot

### Windows(Lite 版)

1. 將 `DinoMutantBot-vX.Y.Z-Windows-Lite.zip` **完整解壓縮**到任意英文路徑
   (❗ 不要直接在壓縮檔視窗內雙擊執行)。
2. 雙擊 **`start-dashboard.cmd`**。
3. 第一次啟動會詢問是否下載 Python 執行環境(約 30MB),請允許;
   之後會自動安裝 OpenCV 等相依套件,約需數分鐘。
4. 完成後瀏覽器會自動開啟儀表板 `http://127.0.0.1:8780`。
5. Dashboard 會留在 CMD 視窗前景執行；關閉該視窗或按 `Ctrl+C` 即可停止，
   不會建立隱藏背景程序或 Windows 登入啟動項。
6. 若要完整移除，請在該資料夾開啟 CMD，執行 `start-dashboard.cmd uninstall`，
   再輸入 `DELETE`；無法立即解除的 Windows 檔案鎖會在重新開機後清除。

### macOS

1. 解壓縮 `DinoMutantBot-vX.Y.Z-macOS.zip`。
2. 雙擊 **`start-dashboard.command`**(若被 Gatekeeper 攔下,右鍵 → 打開)。
   需要先裝好 Homebrew 的 Python 3.12+(`brew install python@3.13`)。
3. 首次啟動自動建立虛擬環境並安裝相依套件,之後開啟儀表板。

---

## 第三步:對上 ADB 埠號

打開包內的 **`app/config.json`**,找到:

```json
"adb": {
  "serial": "127.0.0.1:16384"
}
```

把 `serial` 改成第一步記下的模擬器 ADB 埠號(改完存檔、重啟儀表板)。

- Windows BlueStacks 5 多實例:常見 `127.0.0.1:16384`
- Windows 單實例 / macOS BlueStacks Air:常見 `127.0.0.1:5555`

> ✅ 驗證方式:儀表板的「診斷」按鈕,或終端機執行
> `python main.py --config config.json doctor`,八項檢查應全部 PASS。

---

## 第四步:遊戲內前置

1. Dino Mutant 開到**主畫面**(看得到蛋堆與巢穴的地圖)。
2. 若跳出「**發現不同設備的歷史記錄,您要改為使用此設備嗎?**」→ 按「**是**」
   (同一帳號換機器都會問;注意兩台機器不能同時掛機同帳號)。
3. 把活動彈窗、任務完成橫幅等**手動關掉**(殘留的橫幅可能干擾辨識)。

---

## 第五步:開始掛機

瀏覽器開 `http://127.0.0.1:8780`:

| 按鈕 | 用途 |
|---|---|
| **自動孵蛋＋狩獵** | 主模式:孵化 → 收蛋 → 加速 → 冷卻期間狩獵,循環;快滿時自動篩選+淘汰。S13 與主力共用同一套篩選(Attack/HP 親代 → 頂尖 → 收集 → 洞穴最弱淘汰;量產預設關閉),只有容量門檻不同 |
| **自訂循環** | 自選 Attack/HP/頂尖/量產/收蛋/洞穴；孵蛋冷卻時狩獵，到期自動返家循環。未選洞穴而達安全人口時只繼續狩獵 |
| 開始狩獵 | 純狩獵,不動孵化器 |
| 單階段孵化 | 手動跑單一階段(攻擊/HP 親代、收蛋、洞穴);跑完 10 秒自動回到主模式 |
| 停止 | 優雅停止(做完當前動作才停) |
| 冷卻加速券 | 開關 + 庫存：在自動孵蛋與自訂循環中，每次確認使用成功後隔 30 分鐘再用；到期會安排回訪孵化器，使用後接回原流程。重啟保留計時，無須等待新孵蛋循環或有蛋才使用 |

運轉中儀表板會即時顯示:狩獵/孵化統計、近期事件、目前階段。切換模式直接按新按鈕即可(會自動停舊起新)。

---

## 常見問題

| 症狀 | 處理 |
|---|---|
| `No ready ADB device` | 模擬器沒開 ADB、埠號和 config 不一致,或模擬器剛重啟 — 檢查第三步 |
| 環境檢查(doctor)有 FAIL | 看訊息:缺 Python → 重跑安裝;缺裝置 → ADB 問題 |
| Bot 啟動後只掃描不動作 | 遊戲不在主畫面、解析度不對、或畫面被彈窗擋住 |
| Lite 版下載 Python 失敗 | 防火牆/公司網路阻擋 — 允許連線,或改用 Portable 完整版 |
| 儀表板打不開 | 埠 8780 被占用:關掉舊視窗再啟動 |
| 模擬器解析度跑掉 | 回設定改回 1080×1920 直式,重啟模擬器 |

---

## 進階設定(app/config.json)

| 欄位 | 預設 | 說明 |
|---|---|---|
| `hatch.screening_growth_interval` | 20 | 每比最近一次篩選多幾隻恐龍時，再跑一次完整篩選 |
| `hatch.cull_threshold` | 330 | 達此總數時，篩選後進洞穴清理（遊戲容量仍為 350）；S13 覆寫為 180 |
| `hatch.capacity_limit` | 350 | 各實例的實際洞穴容量；S13 覆寫為 200 |
| `planner.stage_scoped_scan` | true | 分段掃描加速(建議保持開啟) |

祝掛機愉快 🦖
