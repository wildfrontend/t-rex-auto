# 企畫書：自動孵蛋（Auto Hatch）

> 狀態：草稿（2026-07-30 建立）。遊戲內孵蛋規則細節待使用者補充，
> 見文末「注意事件紀錄區」。此文件先定調範圍與架構，不是實作規格。

## 1. 目標

新增第二種自動化功能：自動孵蛋。讓 bot 依遊戲的孵蛋規則自動完成
「收蛋 → 補蛋 → 等待」的循環，減少人工顧孵蛋槽的時間。

## 2. 範圍與定位

- **獨立功能，不與現有自動採集（Auto Hunt）混用。**
  - 各自有獨立的啟動方式（暫定：CLI 子命令或 `--feature hatch` 之類的旗標，待定）。
  - 各自有獨立的設定區塊、素材資料夾、統計計數。
  - 第一版**不做**兩功能同時執行或自動輪替；未來若要輪替（例如狩獵等待
    capacity 時順便去孵蛋），另開企畫。
- 沿用既有框架：`Sense → Think → Act → Verify` 回饋循環、既有的
  Capture / Detect / Action / Recover 基礎設施，以 Feature 方式加入，
  不修改核心狀態機（README 既有原則）。

## 3. 現況架構切入點（初步盤點）

| 既有模組 | 孵蛋功能的利用方式 |
| --- | --- |
| `engine.py` 狀態機（Idle/Capture/Detect/Planning/Action/Verify/Recover） | 直接沿用循環，換掉 Planner 與偵測目標 |
| `detection.py`（template matching / HSV / NMS） | 新增孵蛋相關 UI 素材（蛋槽、收取按鈕、完成標記等） |
| `planning.py` / `planner.py` | 新增 HatchPlanner：決定這一輪要收蛋、放蛋還是等待 |
| `recovery.py` / `stalls.py` | 沿用卡死逃生；孵蛋頁面的重置路徑需另外定義 |
| `status.py` / `status_server.py` | 新增孵蛋統計（收了幾顆、目前槽位狀態、下次可收時間） |
| `config.py` / `config.json` | 新增 `hatch` 設定區塊，與 hunt 設定分開 |

## 4. 流程（依 2026-07-30 使用者提供的截圖與說明確認）

1. **主頁面**（一切行為的起點）：以**固定座標**點擊下方偏中的大蛋堆平台
   （樣式會變，不做圖像辨識）。
   - Verify：看到「孵化器」標題畫面。
2. **孵化器格子頁**（最多 24 顆蛋，一頁約 9 顆，要下滑才能看完）：
   找「蛋下方有『孵化』字樣」的蛋（黃光圈與否無差別）。
   - 找到 → 點第一顆，進入詳細頁（步驟 3）。
   - 目前可見區沒有 → 下滑再掃，直到捲到底。
   - 全部都沒有（只剩藍光圈倒數中的蛋）→ 點 X 回主頁面，等下一輪。
3. **詳細頁循環**（核心迴圈，不用回格子頁）：
   a. 按青色「孵化」按鈕。
   b. 等過場動畫播完，等到結果頁出現「獲取」按鈕。
   c. 按「獲取」（**絕對不可誤點紅色「驅逐」**）。
   d. 自動切到下一顆蛋的詳細頁（計數遞減，如 1/24 → 1/23），回到 a。
   e. 可孵化的蛋處理完後，畫面會**自動回到孵化器格子頁**（計數可能歸零
      如 0/24，也可能還剩冷卻中的蛋，不一定全空）→ 偵測到又回到
      「孵化器」標題畫面就**按 X 關閉**，回主頁面。
4. **回主頁面等待下一輪**：孵化冷卻（約 25 分鐘級）是遊戲限制
   （類比 hunt 的 capacity_wait），不可調短；可調的只有重掃間隔。

## 5. 待確認問題（需要使用者回答）

- [ ] 孵蛋的入口在哪個畫面？從主畫面要點幾層？
- [ ] 孵蛋槽有幾個？槽位數量固定還是可解鎖？
- [ ] 蛋從哪裡來？（狩獵掉落？商店？有限庫存？）要不要挑蛋（稀有度/種類）？
- [ ] 孵化時間多長？畫面上有沒有倒數計時可以讀？
- [ ] 收取時有沒有彈窗/動畫要關閉？
- [ ] 有沒有加速道具？要不要用？
- [ ] 失敗情境：蛋滿、槽滿、庫存空的時候畫面長什麼樣？
- [ ] 啟動方式偏好：獨立命令？控制視窗加選項？`start-bot.cmd` 要不要問？

## 6. 素材需求（草列）

- 孵蛋入口按鈕
- 蛋槽（空 / 孵化中 / 可收取 三種狀態）
- 收取按鈕、確認/關閉彈窗按鈕
- 放蛋相關 UI（蛋清單、選蛋、確認）

實際清單等流程定案後照 `assets/` 既有慣例建立獨立子資料夾（暫定 `assets/hatch/`）。

## 7. 風險與注意

- 孵蛋等待時間可能遠長於狩獵循環（分鐘～小時級），空轉偵測與卡死判定的
  門檻不能沿用 hunt 的數值，需獨立設定。
- 黑畫面暫停 Detect/Verify 的既有機制要確認在孵蛋頁一樣適用。
- 版本更新遊戲 UI 改版時，hatch 素材要能獨立更新，不影響 hunt。

## 8. 注意事件紀錄區（使用者提供，逐條累積）

> 使用者說明的遊戲規則、坑、例外狀況都記在這裡，附日期。定案後回頭修正上面章節。

- 2026-07-30：**所有孵蛋行為都以「主頁面」為起點**。主頁面是主要操作畫面，
  每一輪循環（以及失敗恢復）都應該回到主頁面再開始。
  → 影響：Recover 的重置目標就是「回到主頁面」；需要主頁面的辨識素材。
- 2026-07-30：使用者提供主頁面截圖（村莊/基地畫面）。畫面觀察如下，
  **括號內的解讀是猜測，待使用者確認**：
  - 畫面中央是一片「巢/池」矩陣，多數是藍綠色冰面巢、上面放著白蛋，
    巢邊圍著小恐龍；另有幾個橘棕色空巢（推測：空槽？）和一個深藍色
    碎冰巢（推測：特殊/損壞狀態？）。
  - 部分有蛋的巢外圈有**金色發光圈**（推測：可收取/孵化完成的狀態標記？）。
  - 畫面下方有一座堆滿蛋的大平台（推測：蛋的庫存堆/集中孵化台？），
    周圍有大量小恐龍群。
  - 左側欄：玩家頭像 Lv616、VIP4、`277/350`（推測：恐龍容量？）、
    圖鑑、袋子、**帶紅點的蛋圖示**（推測：孵蛋/蛋清單入口？）、
    兩個 x2 加速圖示、綠色加號。
  - 右側欄：競技場、烤爐、票券、卷軸、行事曆、帶倒數的十字鎬
    （61:33:21）、地圖、信件（紅點）、向上箭頭等按鈕。
  - 上方：任務橫幅「釣魚 0/1」、肉 1,489,366,573、骨頭 529,010、時間 12:03。
  → 待辦：聊天視窗貼的截圖無法直接存檔做素材；之後請把主頁面截圖放進
  repo（建議 `assets/hatch/` 或 `capture/`），或用 bot 的擷取功能抓一張。
- 2026-07-30：**大蛋堆平台（主頁面下方偏中）是可點擊目標，用「固定座標」
  處理，不做 template matching。** 原因：它的樣式會變化（蛋堆外觀不固定），
  以圖辨識不可靠；只要對準座標點下去就好。
  → 影響：設定裡要有這個點的座標（螢幕比例座標，沿用既有的畫面/裝置
  座標映射）；Verify 不能靠「點擊目標長什麼樣」，要改成驗證**點擊後**
  出現的畫面：點下去會開啟「孵化器」全螢幕介面（見下一條），
  所以 Verify 條件＝看到「孵化器」標題畫面。
- 2026-07-30：使用者提供點開蛋堆後的「孵化器」畫面截圖。
  **核心規則：抓「蛋下面有『孵化』字樣/按鈕」的蛋去點擊。**
  畫面觀察（括號內為猜測，待確認）：
  - 標題「孵化器」，旁邊有「升級」按鈕（bot 不要碰）和驚嘆號說明鈕。
  - 標題下方計數「24/24」＝孵化器裡最多 24 顆蛋（使用者確認）。
    一頁只看得到約 9 顆，**要往下滑（swipe）才能看完全部**，
    所以孵化流程必須「掃描目前可見區 → 點完 → 下滑 → 再掃描」
    直到捲到底。
  - 蛋以格狀排列，一頁約 3×3，狀態實質上只分兩種（使用者確認
    黃色光圈與無光圈**沒有差別**，一律只看「孵化」字樣）：
    1. 蛋下方有「孵化」字樣（不論有無黃色光圈）——點擊目標。
    2. **藍色光圈**＋進度條＋倒數（例：00:24:29）——孵化冷卻中，不可點。
  - **整個孵化器都沒有「孵化」字樣時：關閉（點 X）回主頁面，
    等下一個輪迴再來。**（使用者確認）
  - 畫面底部有「冷卻時間加速 50%」按鈕（要花票券，bot 不要碰）、
    紅色 X 關閉鈕（回主頁面用）。
  - 畫面似乎可以捲動（第三排下方還露出半顆蛋）。
  → 偵測策略：以「孵化」文字標籤做 template matching（每顆可孵化的蛋
  下面都有，樣式固定），點標籤或標籤上方的蛋；不要以蛋本身做辨識。
  → 素材需求：「孵化器」標題、「孵化」標籤、X 關閉鈕；倒數/進度條
  狀態可辨識用於「全部冷卻中→等待」的判斷。
- 2026-07-30：使用者提供「點了孵化字樣的蛋」之後的畫面：進入**單顆蛋的
  詳細頁**，不是直接孵化。畫面內容：
  - 上方顯示兩隻親代恐龍（例：Lv208 Dino12610、Lv209 Dino12669，
    各有血量/攻擊/速度數值）。
  - 中間是放在巢上的蛋（金色光暈）＋計數「1 / 24」，右邊有 ▶ 箭頭
    （可切到下一顆蛋）。
  - 下方有青色**「孵化」按鈕**＋紅色 X（取消/關閉）。
  - **操作＝在這頁再按一次「孵化」按鈕**才會真正孵化。
  → 流程更新：孵化器格子頁點蛋 → 詳細頁 → 按「孵化」按鈕。
    詳細頁的「孵化」按鈕樣式固定（青色圓角鈕），適合 template matching，
    也是點蛋成功與否的 Verify 條件（看到詳細頁＝點蛋成功）。
  → 待確認：詳細頁的 ▶ 箭頭能不能用來連續處理下一顆蛋（可能比回格子頁
    再找「孵化」標籤更有效率）。
- 2026-07-30：按下詳細頁「孵化」後：**先播一段過場動畫**，之後變成
  **孵化結果頁**，這一頁要按「獲取」。畫面內容：
  - 破殼的新恐龍（例：Lv211 Dino12741）＋數值（血量 2050、攻擊 2、
    速度 4 (+3)，(+3) 旁有藍色特殊圖示，推測是變異加成——bot 不用管）。
  - 名字旁有鉛筆（改名）與圖鑑小圖示——不要碰。
  - 星星、鈴鐺兩個開關——不要碰。
  - 底部兩顆按鈕：青色**「獲取」**（要按的）、紅色**「驅逐」**
    （絕對不能按——會把恐龍趕走）。
  → 實作注意：過場動畫期間畫面持續變化，Detect 要等到「獲取」按鈕
    出現才動作（等待逾時要設得比動畫長）；「獲取」「驅逐」都是固定
    樣式按鈕，適合 template matching，但點擊前必須確認比對到的是
    「獲取」，避免誤點「驅逐」。
  → 待確認：按「獲取」之後回到哪個畫面（孵化器格子頁？詳細頁？）
    →（已確認，見下一條）按完會自動換到下一顆蛋的詳細頁。
- 2026-07-30：**按「獲取」後會自動切到下一顆蛋的詳細頁**（截圖：
  計數從 1/24 變 1/23，親代換成另一組），所以核心循環不用回格子頁：
  **詳細頁按「孵化」→ 過場動畫 → 結果頁按「獲取」→ 自動下一顆
  → 再按「孵化」→ …不斷輪迴**，直到沒有可孵化的蛋。
  → 格子頁的「找孵化字樣＋捲動」只是**入口**（挑第一顆點進去），
    之後都留在詳細頁循環，效率高很多。
  → 待確認：輪到「冷卻中的蛋」時詳細頁長什麼樣（沒有「孵化」按鈕？）；
    →（已部分確認，見下一條）循環結束會自動回到格子頁。
- 2026-07-30：使用者提供「孵化循環跑完之後」的截圖：**畫面會自動回到
  孵化器格子頁**（截圖例：計數 0/24、所有蛋槽都是虛線空槽），
  使用者確認**不一定全空**——可能還留著藍光圈冷卻中的蛋。
  **這時的正確動作＝直接按 X 關閉**，回主頁面等下一輪。
  → 實作：詳細頁循環的離開條件不是在詳細頁等不到「孵化」按鈕，
    而是**偵測到畫面回到「孵化器」標題的格子頁**；此時不用再掃
    「孵化」字樣（能孵的剛剛都孵完了），直接點 X。
  → 素材補充：空蛋槽是虛線描邊樣式，可作為「格子頁」的輔助辨識，
    但主要靠「孵化器」標題判斷即可。

## 9. 里程碑（暫定）

1. **M0 規則收集**：本文件第 5 節問題全部有答案，第 8 節事件整理完。
2. **M1 素材與偵測**：孵蛋頁 UI 素材齊備，Detect 能正確辨識槽位狀態。
3. **M2 MVP**：能完成「收取已完成的蛋」單一動作循環。
4. **M3 完整循環**：收蛋＋放蛋＋等待，含失敗恢復與統計。
5. **M4 佈署**：整合啟動器/控制視窗、文件、部署到 D:\DinoMutantBot。
