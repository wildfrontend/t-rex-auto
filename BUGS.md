# 已知問題

## 已修正：信箱已滿會讓狩獵流程卡住並觸發不必要的遊戲重啟

**影響版本：** v0.2.11、v0.2.14  
**修正版本：** v0.2.15（2026-07-26）

### 重現條件

1. 在地圖選定目標並開啟狩獵確認視窗。
2. 點擊「狩獵」時，遊戲顯示「您的信箱已滿！請先清空您的信箱。」。
3. 狩獵確認視窗仍留在畫面上，需先按紅色 `X` 才能回到地圖並進入信箱。

### 原因

- `hunt_confirm_button` 的驗證因地圖轉場沒有出現而反覆失敗。
- 重試耗盡後，規劃器只抑制確認按鈕，沒有把仍可見的
  `hunt_dialog_close_button` 解讀為信箱容量阻擋。
- Bot 因此持續回報沒有可操作目標，最後由無狩獵進度看門狗重啟遊戲。

### v0.2.15 修正

- 驗證狩獵確認時一併偵測 `hunt_dialog_close_button`。
- 只有在狩獵確認重試耗盡且紅色關閉鍵仍可見時，才啟動信箱滿恢復流程。
- 自動關閉狩獵視窗、開啟信箱、全部獲取、確認獎勵並關閉信箱。
- 清除舊的確認按鈕抑制狀態，回到地圖後繼續狩獵。
- 記錄 `mailbox_full_recovery` 事件，方便日後由診斷紀錄辨識。

### 驗證

- 正常狩獵確認仍優先執行，不會因為畫面有紅色關閉鍵就誤收信。
- 模擬實際截圖狀態時，操作順序為：
  `hunt_dialog_close_button` → `mailbox_button` →
  `mail_collect_all_button` → `mail_reward_collect_button` →
  `mail_close_button`。
- 完整回歸測試通過。
