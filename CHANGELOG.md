# 版本紀錄

## v0.0.28 — 2026-08-13

- 主力完整孵蛋流程恢復四階段管理：Attack、HP 親代篩選後，依序執行頂尖
  （最佳屬性組合）與量產（等級）自動放置，再收集所有蛋。非頂尖／量產階段
  出現的殘留自動放置確認框仍會按「否」安全取消。
- 修正主力預設人口上限與實機不符：`hatch.capacity_limit` 改為 `350`，避免實際讀到
  `300/350` 時被視為 `unexpected_capacity` 而反覆執行洞穴回中。容量仍是每個實例
  可獨立設定，其他上限為 `380` 的使用者可保留自己的覆寫值。
- S13 新手孵蛋＋狩獵每次從狩獵確認回到主頁後，會額外打開「我的巢」並收集一次蛋，
  再接續原本孵化倒數或檢查孵化器；這趟只收集、不重做自動放置，也不會重置倒數。
- 修正主力進入遊戲時，「發現不同設備的歷史記錄」視窗雖已辨識出左側「是」，卻又
  被廣義版面偵測誤判成自動放置確認框而停住。明確的設備歷史／登入提示按鈕現在優先
  於自動放置防護處理；真正的主力自動放置殘留視窗仍只會按「否」。
- 修正 S13 新手孵蛋倒數期間雖已切換到狩獵，卻因獨立實例的精簡設定沒有繼承主設定，
  導致規劃器看得到森林按鈕與恐龍但無法選取。`instances/<id>/config.json` 現在會自動
  疊加 `app/config.json`（原始碼環境則為根目錄 `config.json`），並保留各實例的覆寫值。

## v0.0.27 — 2026-08-13

- 修正 S13 新手孵蛋在「我的巢」畫面把綠色自動放置按鈕誤判成離線成長結果頁捷徑，
  導致以 `hatch_startup_nest_shortcut` 名義反覆點擊同一區域。巢穴標題可見時不再執行
  該啟動捷徑；每輪仍只正式按一次自動放置，隨即鎖住直到進入收集階段。

## v0.0.26 — 2026-08-13

- 主力完整孵蛋／孵蛋狩獵流程只執行 Attack、HP 親代篩選，不再排入任何自動放置
  階段；若接手時已有自動放置確認框，會明確按「否」並安全回到主畫面。
- 修正「我的巢」白色面板與綠色自動放置按鈕被誤判為離線成長結果頁，導致主力以
  `hatch_startup_nest_shortcut` 名義誤點自動放置。巢穴標題可見時一律禁止該捷徑。
- 新手流程辨識「沒有可供自動放置的巢穴」後不再重複按自動放置，提示消失後仍會
  繼續執行一次收集；自動放置確認框只由新手流程按「是」。
- 新增可設定的 `hatch.capacity_limit`，主帳與 S13 預設為 `380`，人口讀值與清理門檻
  依實際容量驗證，不再把 `369/380` 當成格式異常。

## v0.0.25 — 2026-08-13

- 主帳與 S13 改用獨立的 config root：S13 設定移至
  `instances/s13/config.json`，其 log、資料庫、截圖與素材皆不再與主帳共用。
- Dashboard 依實例限制可啟動模式：主帳不會顯示或接受新手流程；S13 不會顯示或接受
  主帳的完整孵蛋＋狩獵流程。

## v0.0.24 — 2026-08-13

- 修正主帳與新手帳皆會遇到的自動放置確認框變體：遊戲顯示「最佳屬性」或「等級順序」
  都不再影響流程。自動放置後只要辨識到明確的「是」按鈕，就安全按一次並繼續收蛋。

## v0.0.23 — 2026-08-13

- 修正 S13 獨立設定未指定 ADB 擷取，造成啟動後錯誤使用桌面視窗擷取、立即回報
  `No visible emulator window found` 的問題。現在 S13 固定使用 `capture.backend: "adb"`。

## v0.0.22 — 2026-08-12

- 新增 `hatch-beginner-hunt`：新手帳號照固定流程「孵化 → 所有 → 自動放置一次 →
  確認『是』→ 收集一次」，僅在孵化倒數期間狩獵，倒數最後 30 秒回到巢穴。
- Dashboard、CLI 與 Windows Hatch runner 都可啟動新手孵蛋＋狩獵。新手管理動作若
  未驗證會安全停止整個模式，不會改成繼續狩獵；不讀素質、不篩選、不進洞穴或淘汰。

## v0.0.21 — 2026-08-12

- 新增獨立的新手帳號設定 `config-s13.json`，使用 ADB
  `127.0.0.1:16416`，並保存新手人口上限 `200`。既有的 `config.json` 維持為主帳設定。
- Lite 發佈包與 Dashboard 實例清單加入「新手 S13」實例，使用獨立 status port `8775`。

## v0.0.20 — 2026-08-12

- 修正新手自動孵蛋把孵化結果畫面的藍綠色區塊誤判成「自動戰鬥」提示。現在只要可見
  「領取」或「放逐」按鈕，就會優先處理孵化結果，不再反覆誤點左側畫面並等待驗證逾時。
- 保留自動放置確認框的安全流程：偵測到確認框與「是」按鈕才點一次「是」，不會勾選
  「下次不要再問」，回到巢穴後才進行一次收集。

## v0.0.19 — 2026-08-12

- 修正新手與完整孵蛋模式在孵化器格狀清單中只辨識到「孵化」按鈕、卻漏掉孵化器
  標題狀態的問題。現在可正常逐顆孵化，無可孵蛋時也會正常關閉視窗進入後續流程，
  不再持續輸出 `Planning | no actionable target`。

## v0.0.18 — 2026-08-12

- 新增「新手自動孵蛋」模式：逐顆孵蛋後，巢穴固定執行「所有 → 自動放置一次 →
  收集一次」，不讀親代素質、不排序、不進洞穴、也不淘汰恐龍。
- 新手模式辨識「孵化器已滿」提示為正常收集結果，會離開巢穴並等待下次孵化，不會
  重複按收集或自動放置；任何自動放置／收集的未驗證結果都會安全停止。
- Dashboard、CLI 與 Windows Hatch runner 均可啟動 `hatch-beginner`。每個實例設定檔
  預留獨立的 `hatch.beginner_population_limit` 與
  `hatch.beginner_stat_upgrade_guards`，目前僅保存設定，尚不介入新手流程。

## v0.0.17 — 2026-08-12

- 修正 Windows 控制器啟動或重啟後過早回報成功。現在只有新 Bot 的 `/health`
  身分、監聽 PID、`main.py`／設定檔／status port 命令列與 `/status` 全部驗證通過，
  才會回報 `started`；runner 提前退出或 60 秒內未就緒都會回傳明確錯誤。
- Windows 乾淨重啟會先確認舊 Bot 的確切 PID 已退出，並實際測試 status port 已可
  重新綁定後才啟動新程序。停止逾時、port 被其他程序接手、或沒有 LISTEN PID
  但仍無法綁定時，都不會繼續啟動第二個 Bot。
- Port 被占用時顯示占用程序的 PID、名稱與命令列；只允許控制通過 Dino Bot API、
  PID、`main.py`、config 與 status-port 全部一致的程序，未知程序不會被自動終止。
- Dashboard 不再吞掉背景啟動與模式切換例外。控制區會持續顯示處理中、成功或失敗
  狀態及錯誤代碼；Windows runner 也會把 Python runtime、設定檔與重複程序等啟動
  失敗原因保存到實例 log，供 Dashboard 直接顯示。

## v0.0.16 — 2026-08-12

- 修正模擬器重連後仍被 `adb devices` 列為 `device`、但第一個
  `adb shell` 回覆 `error: closed` 時所有 Bot 模式立即崩潰。現在只對這個明確的
  transport 錯誤執行一次有上限的 reconnect 並重試原命令；啟動前 doctor 與引擎
  建立時也會驗證 shell 與裝置尺寸，持續失敗時在開始任何操作前要求重開模擬器。
- 親代替換改為「主屬性只要更高就換」。先前候選必須高出 `min_delta`~`max_delta`
  才算數,實機出現攻擊 389 的親代放掉旁邊 390 的情況;更嚴重的是上限那一側 ——
  高出 +8 以上會被當成 OCR 異常略過,**候選越強越容易被跳過**。
- `stat_upgrade_guards` 的 `min_delta`／`max_delta` 退役:仍可讀取以相容舊設定,
  但不再影響任何判斷,設定檔若還留著會在啟動時警告一次。擋掉不可能讀值的
  `min_value`／`max_value`／`multiple_of` 維持不變。
- 這組設定原本是「親代替換防呆」(攻擊 +1~3),後來改成 +3~7 並在儀表板標為
  「異變安全範圍」,但全專案沒有任何地方在驗證異變 —— 同一個鍵被當成兩件事用。
- 新增 ADB 連接埠自動偵測。`adb.serial` 留空時,啟動會探測已知模擬器連接埠
  (BlueStacks/MuMu/LDPlayer/夜神/MEmu 等),**恰好找到一台**才採用;找到多台會
  列出來要求指定,不會替使用者猜。先用 TCP 確認埠有人在聽再 `adb connect`,
  因為對死埠 `adb connect` 每個要阻塞約一秒。
- `adb.serial` 設錯時的錯誤訊息會列出「現在實際連得到什麼」。舊訊息只重複那個
  失敗的埠 —— 那正是使用者已經知道、且無從下手的唯一資訊。
- 新增 `dino-bot adb` 指令:列出掃到的裝置、`--auto` 在只有一台時寫進設定、
  `--set` 指定、`--clear` 改回自動選擇。儀表板的實例設定也加了「掃描」按鈕。
- 新增設定 `adb.auto_discover`(預設 true)與 `adb.discovery_ports`(自訂掃描
  範圍);Windows 可攜版預設 `serial: null`,拿到包的人不必先知道自己的埠。
- 修正 Windows 批次檔全部是 LF 換行:`start-dashboard.cmd` 同時使用 `^` 續行與
  跨行 `if (...)` 區塊,這兩種語法在 LF 下會被 cmd.exe 解析錯誤,症狀就是雙擊後
  視窗一閃就關、沒有任何訊息。新增 `.gitattributes` 強制 `*.cmd`/`*.bat`/`*.ps1`
  為 CRLF、`*.sh`/`*.command` 為 LF,不再依賴編輯器習慣。
- 新增 Windows 可攜版打包 `scripts/windows/package-windows-portable.sh`:Python
  runtime 與相依套件隨包附上,解壓縮後雙擊即可執行,不需要網路也不需要安裝。
  Lite 版首次啟動要下載約 60MB 並執行 pip,任何一步失敗都會讓啟動中斷。
- 可攜版啟動器 `start-dashboard-portable.cmd` 的每一條失敗路徑都會顯示原因、
  暫停視窗並寫入 `logs\launcher.log`,並會辨識「直接從 ZIP 裡執行」這個最常見的
  誤用。關鍵訊息中英並陳,以免字碼頁問題讓錯誤訊息本身變成亂碼。
- 可攜版剔除用不到的 opencv 影片編解碼 DLL、haarcascade 資料與 pywin32 的
  COM/IDE/文件(合計約 53MB);打包前檢查 runtime 檔案齊全且批次檔為 CRLF。

## v0.0.15 — 2026-08-12

- 修正孵蛋回主畫面的復原流程反覆進出狩獵地圖:「進森林再退出」不會移動鏡頭,
  卻沒有次數上限,每一次 Back 額度都會再換來一趟來回,外層重試又把計數歸零。
  實測一次事故切換地圖 18 次、空轉 111 秒後 Bot 自行停止;現在整段最多 2 次。
- 回中修正改用實測偏移量:判定門檻是 100px,原本卻一律重放 450px 的校準滑動,
  偏移落在 100–350px 之間必定修過頭。修過頭會把蛋堆與主畫面錨點一起推出畫面,
  之後所有能量測位置的分支都失效,復原再也回不了頭。
- 復原流程若因為自己的滑動而失去畫面上所有定位參考,會在用盡既有脫困手段後
  反向重放該滑動。轉場中的空白畫格不會觸發它,以免撤銷掉實際有效的修正。
- 復原失敗時保存畫面到 `logs/stalls/home-recovery-*.png`(綠圈為蛋堆底座應在的
  位置、藍圈為實測位置),並納入診斷包。此前這類故障不算 blind stall,不留任何
  畫面證據,事後無法判斷是地圖被平移還是模板失效。
- 整段復原(含重試)加上 45 秒上限,並修正測量式微調會誤推進洞穴回退段索引、
  導致後續洞穴回中只走一半的問題。

## v0.0.14 — 2026-08-11

- Dashboard 新增 HP／攻擊異變安全範圍設定，預設更新為 HP +30～70、攻擊 +3～7；
  各模擬器可分別儲存，下一次啟動 Bot 時生效。
- 親代與候選的 HP／攻擊／速度只要仍能辨識成數字，就不再因超出防呆範圍而中止
  Attack／HP 篩選；異常親代保留原親代，異常候選直接略過並繼續後續流程。
- 修正 HP 特化親代速度 `150` 被 OCR 誤讀為 `750` 後卡在
  `parent_stats_unreadable`；速度超過設定上限時改以該上限繼續，並保留警告 Log。

## v0.0.13 — 2026-08-11

- 各操作加入統一的連續失敗升級鏈：第一輪保留局部處理，第二輪執行狩獵置中或
  孵蛋安全回主畫面，第三輪起重啟遊戲；已重啟三次仍再次失敗時安全停止 Bot。
- 完成狩獵或成功領取孵化結果會清除失敗與重啟計數；Log 新增
  `failure_escalation`，記錄層級、階段、目標、失敗輪數與自救動作。
- 狩獵按鈕連續兩次點擊後畫面未變時提早放棄該座標並重新規劃，避免停留在
  看似高信心但實際無效的舊偵測位置。
- 狩獵流程減少不必要的完整模板掃描；HP／攻擊親代最高值相同時，優先保留其他
  屬性較低的個體，降低非目標屬性被一併拉高的情況。
- Windows 啟動失敗時保留完整例外與 traceback，方便從 Log 直接判斷缺少模組、
  設定錯誤或初始化失敗原因。

## v0.0.12 — 2026-08-10

- 狩獵地圖的恐龍點擊位置由等級文字下方邊緣移入身體區域，降低新版地圖上點到
  恐龍外圍而未開啟狩獵視窗的情況。
- 恐龍點擊失敗後直接在最後一張驗證畫面重新辨識並規劃下一次點擊，不再先重新
  截圖；狩獵持續有目標時也不再每 30 秒強制執行耗時的完整模板掃描。
- 限頻保存恐龍點擊失敗前後的標記畫面與辨識資料至 `logs/stalls`，方便後續依實機
  案例繼續校正，且沿用既有數量上限避免長時間執行累積大量檔案。

## v0.0.11 — 2026-08-10

- 孵蛋流程首次讀到洞穴總數時先完成一次巢穴篩選；之後改以最近一次篩選的
  總數為基準，每增加 20 隻才重跑篩選。取消固定 8／12 隻孵化批次。
- 清理安全門檻調為 `330/350`：到門檻時先完成篩選，再進洞穴選最弱恐龍進行
  連續戰鬥；遊戲 HUD 的實際容量分母仍維持 `/350`。
- 收集所有蛋固定沿用原本的 My Nest 流程，不再操作主畫面的收集蛋按鈕。
- 重新校正狩獵地圖與自動放置下拉選單，適應新版 UI 的 HUD、右側操作區與選單位置。

## v0.0.10 — 2026-08-10

- 任何座標連續兩次點擊後畫面像素零變化，就改按硬體返回鍵脫困。引擎本來就有這道
  護欄，但驗證流程只要目標宣告了預期的下一個畫面，失敗時就提早返回、不計算
  `pixel_change`，導致護欄永遠被跳過；現在在該分支返回前先量測並一併回報。
- 這與 v0.0.9 的「孵化點擊無反應時重新檢查 `N/350`」互補：後者處理容量滿導致
  點不動的特例，前者處理座標本身失效、App 無回應或遮罩吃掉點擊的情況，且適用於
  所有目標而不只是孵化點。
- 診斷包中 `hatch_egg_pile (457,1279)` 曾橫跨三個工作階段被點了 19 分鐘、送出
  36 次操作零成功，期間連續 18 幀偵測結果連信心值小數第四位都相同；修正後第三次
  點擊就會改送返回鍵。畫面確實有變化（例如誤觸彈出確認框）時不會誤觸發。

## v0.0.9 — 2026-08-10

- 修正 HP 小字體 `0` 因底部反鋸齒短尾而被辨識成 `6`；在像素分數接近時，改用
  封閉孔洞的高度區分完整 `0` 與只有下半部圓環的 `6`。實機左右親代 `2920`
  採證已可同時正確讀取，並保留真正 `276` 與洞穴 `/350` 的回歸測試。
- 同一 Attack／HP 親代校正錯誤連續恢復三輪後會封鎖孵化，不再每分鐘關閉、重開
  My Nest；混合模式會自動切回狩獵，單獨孵化模式則安全停止。
- My Nest 開啟動作耗盡四次驗證重試時，同樣封鎖孵化並停止無限點擊。
- 孵化點擊無反應時先重新連續讀取洞穴容量；`350/350` 先完成四階段篩選與清除，
  容量未滿才回來有限次數重試孵化點。

## v0.0.8 — 2026-08-10

- HP OCR 新增 10 的倍數防呆，像 `2926` 這類不符合遊戲規則的讀值會直接拒絕，
  不會被當成可替換的親代或候選。
- 親代與候選數值必須連續兩個完整畫面一致才採信；三次校正仍不一致時安全停止該階段，
  親代失敗會保留校正採證，不執行替換點擊。
- 保留 HP +10～30、攻擊 +1～3 的升級範圍檢查，搭配連續讀值避免單幀將
  `2960` 誤判為 `2900` 後做出錯誤替換。
- 完整巢穴管理改為累積孵化 8 顆後才執行；少量新蛋只收蛋並累計，不再每 1～2 顆
  重跑 Attack／HP／Top／Mass 四階段。
- 洞穴容量預警低於實際淘汰線時不再反覆觸發完整篩選；估算到 350 時仍會立即處理，
  且清除前強制確認四個篩選階段都有完成紀錄。

## v0.0.7 — 2026-08-09

- Dashboard 支援多個 Bot 實例，各自使用獨立的 ADB serial、status Port、設定、日誌、統計與庫存。
- 可直接從 Dashboard 新增實例，並在介面修改名稱、ADB serial 與 Port。
- 儲存實例設定時，執行中的 Bot 會安全停止並以新設定重新啟動；同一實例仍禁止重複啟動。
- Windows Lite 打包與部署流程加入實例註冊表，更新程式時保留使用者既有的 `instances.json`。
- 偵測到重複登入提示時，會先按下 `CLOSE`；確認提示已關閉後，自動重啟遊戲。
- Dashboard 切換到單階段工作時，會等待舊 Bot 程序完整退出後再啟動，避免狀態 API
  先關閉造成新工作未啟動；Bot 的即時日誌仍顯示在各自的 CMD 視窗。
- Dashboard 啟動時會實際匯入必要模組來驗證內嵌 Python；runtime 缺檔或損壞時會自動
  重新進入安裝流程，不再只因 `python.exe` 存在就誤判為可用。

## v0.0.6 — 2026-08-08

- 親代 HP／攻擊／速度讀取失敗時，自動保存完整畫面與六個數值 ROI 的原圖、二值化放大圖。
- 採證 JSON 記錄失敗階段、左右側、嘗試次數與每個欄位的原始辨識字串，方便後續調整
  ROI 與字模。
- 採證沿用限流與保留數量上限，不會因長時間卡住產生大量檔案，也不會改變讀取失敗時拒絕點擊的安全行為。
- 洞穴恐龍清理門檻由 320 調整為 350，並改為數量達到門檻時觸發，避免 350/350 時跳過清理。

## v0.0.5 — 2026-08-07

- 洞穴容量在裁切畫面讀取成功後立即保存；回中驗證失敗並復原時，直接續跑尚未完成的
  Attack／HP／Top／Mass 篩選，不再重複容量預檢。
- 容量必須連續兩幀讀到相同的 `N/350` 才採信；讀數變動時重新累計，避免單幀 OCR
  誤判觸發篩選或淘汰。
- 數字字模加入封閉圈拓撲校正，改善 `5/6/8` 分數接近時的誤判；實際 HP 候選畫面從
  `2540/2840` 修正為 `2640`。
- My Nest 偵測依工作階段從固定 55 種模板縮減為 7–34 種，降低 Attack／HP／Top／Mass
  與收蛋流程的每幀辨識時間。
- 孵化領取後若直接進入未完成蛋詳細頁，改由畫面結構驗證成功，避免等待逾時與假失敗。

## v0.0.4 — 2026-08-06

- 新增可調整的親代素質防呆：HP 升級差值 10–30、攻擊差值 1–3、速度有效值
  1–150；OCR 讀值或候選差值超出範圍時拒絕替換。
- 防呆範圍集中在 `hatch.stat_upgrade_guards`，後續遊戲版本擴大數值時可直接修改
  設定，不需更動替換流程。

## v0.2.24 — 2026-07-29

- 新增本機白名單 `POST /control/restart-game`，讓 AI 不必操作 CMD 選單即可要求 Bot
  重啟固定的 Dino Mutant App；不接受任意 package、activity 或 ADB 指令。
- 重啟請求會排入 Bot 主迴圈，避免 HTTP 執行緒與狩獵動作同時使用 ADB。手動請求會略過
  自動復原冷卻時間，完成後清理暫存工作流狀態。
- `control-windows.ps1` 與 `control-dino-bot` Skill 新增 `restart-game`，維持本回合明確
  授權、`-Confirm`、本機 API 身分及程序身分驗證，並用狀態計數確認執行結果。

## v0.2.23 — 2026-07-29

- 日誌與事件流改成保留多個世代，第二代以後 gzip 壓縮。原本兩者都只留一代，
  事件流 16 MB 上限約 97 分鐘就寫滿，等於只保得住約三小時；一次七小時的通宵
  執行，前面五個半小時在有人要看之前就被覆蓋掉了。
- 新增 `event_log.backup_count`（預設 20）與 `log_backup_count`（預設 12）。
- 實測壓縮率：事件流 6.5%、文字日誌 4.4%，所以 20 代事件只佔約 52 MB 而非 320 MB。
  合計磁碟從 96 MB（涵蓋約 3 小時）變成 131 MB（事件涵蓋約 34 小時、文字約 26 小時）。
- **最新的那一代刻意不壓縮。** 診斷包用 `events-*.jsonl` 和 `20*.log` 兩個 glob
  讀歷史，壓縮世代的檔名不符合這兩個樣式，所以既不會被當文字讀進去、也不會被
  誤打包。回歸測試直接斷言這兩個 glob 的結果。
- 世代編號愈大愈舊（`.1` 最新），沿用 logrotate 慣例與原本 `.1` 的語意。

## v0.2.21 — 2026-07-29

- `planner.action_cooldowns_ms.no_available_dinosaurs` 從 90 秒改成 30 秒。
- **不要再調高 `own_hunt_path.min_area`。** v0.2.20 把它從 8 調到 50 試過了，
  結論是否定的，該版本已作廢（分支 `experiment/own-path-min-area`，commit `47e2533`）。
  動機是 `own_path_marker` 佔所有拒絕理由的 45.5%（6655 次），每幀中位 32 個路線標記、
  每個挖掉半徑 90px；用 3489 幀回放，被擋掉的恐龍比例 min_area 8 是 31.4%、30 是 19.6%、
  50 只剩 2.1%，看起來像是擋過頭。
  實測推翻了這個假設：`duplicate_hunt_alert` 在先前三輪 3489 幀裡出現 **0 次**，
  min_area=50 跑 15 分鐘就出現 **46 / 321 幀（14.3%）**，開機後 0.7 分鐘就發生且全程持續。
  30–50 px² 那批小斑點是真的路線標記，不是雜訊；`own_path_marker` 擋掉四分之一到三分之一
  的恐龍是它該做的事。連帶代價：恐龍成功率掉到 58.9%（同期對照 66.0% / 69.7%）、
  狩獵掉到 293 次/小時（三輪最低）、46 個規劃循環卡在 `blocked`。

## v0.2.19 — 2026-07-28

- 新增 `planner.max_center_distance_px`：恐龍離畫面中心超過這個距離就不點，預設 600。
  點下恐龍會讓地圖置中到牠身上，離中心愈遠、那一下愈常只移動地圖而不開狩獵面板。
  實測 161 分鐘 1251 次點擊：300 px 內成功率 86%、300–500 px 69%、500 px 外只剩 21%，
  600 px 外的 31 次點擊只換到 2 次狩獵。拒絕理由記為 `center_distance`。
- 確認過三個反面假設，都不是主因，所以沒有改動對應參數：判定失敗後 6 秒內
  `hunt_button` 有 98.7% 從未出現（不是 verify 期限太短）；重試間隔拉長成功率
  從 53% 掉到 25%（不是重試太急）；信心值 0.60–0.65 這段成功率 59%，收緊門檻
  會賠掉的成功次數多於省下的白工。

## v0.2.18 — 2026-07-28

- 依 v0.2.17 實際執行事件紀錄，縮短信箱容量阻擋狩獵確認時的恢復路徑。
- 第一次狩獵確認失敗仍保留一次重試；第二次仍停在對話框時直接關閉並清理信箱。
- 避免每次容量阻擋都浪費第三、第四次確認與後續重試耗盡。
- `planner.action_cooldowns_ms.target_too_strong` 從 300 秒降到 30 秒。
- 新增兩次阻擋短路與缺少對話框證據時不誤判的回歸測試。

## v0.2.17 — 2026-07-27

- 錨點規則改成只在「中央蛋真的被偵測到」時才套用。回中的目的是補充可點的恐龍供給,
  中央蛋只是「重置完成了」的訊號,不是要保護的東西。實測一輪 19 分鐘裡,
  `anchor_window` 用推算出來的錨點淘汰了 1344 個候選、清空 22% 的規劃循環,
  然後再花 11% 的循環去重置一張根本沒空的地圖。`own_path_angle` 同樣處理——
  它的角度是從錨點量的,錨點是推算的時候這條規則只是雜訊;防重複派隊由
  `own_path_marker` 接手,路線標記每幀 30 個、95% 的幀都有,本來就濾掉四分之一的恐龍。
- 候選排序改用「離畫面中心多遠」而不是「離錨點多遠」。點下去地圖會置中到那隻恐龍,
  所以離畫面中心的距離就是地圖即將移動的量。剛重置完蛋就在畫面中心,兩者一致;
  漂移之後這個才是對的。位移愈小,一次重置能派出的次數愈多。
- 新增 `planner.recenter_min_candidates`:通過所有規則的恐龍少於這個數量才重置。
  重置從「這個 cycle 沒挑出目標」改成看實際供給量,緩衝期保留。
- `plan` 事件新增 `supply` 與 `anchor`(measured/predicted)兩個欄位。
- 新增 `planner.blind_idle_seconds`：規劃器選不出目標又講不出自己在等什麼時，
  20 秒後強制解除所有卡住的階段並接手畫面中心當錨點。原本每個逃生條件都寫成
  「看到某個控制項才放行」，而卡死的定義正好是那些控制項一個都看不到；沒有錨點時
  狩獵分支又會把同一個畫面判成「在採集地圖但錨點掉了」，重新武裝同一個回中，
  於是繞圈。拿實測 log 回放，兩段分別 157 秒與 241 秒的卡死都在 23 秒內恢復，
  而且是靠點掉一隻已偵測到的恐龍恢復，不是盲點座標。
- 卡死當下自動存下畫面到 `logs/stalls/`，附帶該幀偵測到什麼的 `.json`。
- 新增 `planner.mail_stage_timeout_seconds`：收信流程停止推進 20 秒就放棄本輪。
  實測一段 55 個 cycle 的空轉全部耗在等一個不在畫面上的信箱按鈕。
- `recovery.no_hunt_progress_timeout_seconds` 從 180 降到 90。重開 App 現在是最後
  手段而不是唯一手段，可以更早動作。
- 診斷新增 `blind_stalls`、`stage_idle_cycles` 與 `detection_visibility`：分別回答
  卡死多久、某階段是在做事還是空等、每個控制項多常真的出現在畫面上。
- 大按鈕改在半解析度比對，恐龍標籤維持原尺寸：一次全掃描 1667 ms 降到 492 ms。
- 規劃階段只掃目前階段用得到的素材，跳過只有登入時才會出現的三個對話框。
- 連續兩個 cycle 規劃不出目標、或每 30 秒、或遊戲重啟後，掃描自動放回全部。
- 卡住判定從幀數改為秒數（`planner.stalled_recenter_seconds`），不再隨主機負載飄動。
- 新增 `idle_ms` 與 `recenter_reason` 診斷欄位，可分辨排程回中與餓死回中。

## v0.2.16 — 2026-07-27

- 保留 Windows 啟動器原有的 `fast` 預設，使用者仍可選擇 `safe` 或自訂時間。
- 操作後改用可取消的條件式驗證：下一個 UI 出現便立即繼續，慢速電腦則等待到上限。
- 新增 `verify.minimum_checks`，避免一次較慢的擷取直接耗盡整個驗證期限。
- 診斷統計分開 `pending` 輪詢與 `final` 結果，不再把等待中的未命中算成操作失敗。
- 回森林驗證必須看到森林按鈕消失，並接受恐龍畫面作為成功轉場證據。
- 蛋錨點與地圖標誌漏偵測時，連續兩幀恐龍畫面可安全解除 recenter 卡死。

## v0.2.15 — 2026-07-26

- 修正信箱已滿時，狩獵確認重試耗盡後卡住並反覆重啟遊戲。
- 新增以實際失敗畫面為依據的信箱滿恢復狀態。
- 自動關閉狩獵視窗、全部收取信件並恢復狩獵。
- 新增 `mailbox_full_recovery` 診斷事件與回歸測試。
