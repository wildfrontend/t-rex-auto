# 猛龍計畫使用教學

這份教學適用於 Windows 電腦上的 BlueStacks 與 Dino Mutant。Bot 會透過
ADB 讀取遊戲畫面並操作，不需要搶走滑鼠或鍵盤焦點。

> 請自行確認遊戲規則、帳戶安全與使用風險。本工具不會提供帳號或登入資料。

## 1. 需要準備什麼

1. Windows 10 或 Windows 11。
2. BlueStacks 5，並已安裝、登入 Dino Mutant。
3. 完整的 `DinoMutantBot` 資料夾；請不要只複製其中的 `app` 資料夾。

Bot 已內建 ADB（位於 `app\tools\platform-tools\adb.exe`），不需要另外安裝
Android SDK Platform Tools。

完整分享版已附帶可攜式 Python，不需要另外安裝。若資料夾未封裝 Python，第一次
雙擊時也會詢問是否自動下載安裝。

## 2. 複製與放置 Bot

將整個資料夾複製到本機磁碟，例如：

```text
D:\DinoMutantBot-App
```

建議將資料夾直接放在 `D:` 根目錄，不要放在 OneDrive、桌面或路徑含特殊字元的資料夾。

## 3. 設定 BlueStacks

1. 開啟 BlueStacks，進入「設定 → 進階」。
2. 開啟 Android Debug Bridge（ADB）。
3. 確認 ADB 位址為 `127.0.0.1:5555`。
4. 將遊戲畫面設為直向 `900 × 1600`。Bot 的辨識素材以此畫面建立。
5. 啟動 Dino Mutant，並停在主地圖或採集地圖。

BlueStacks 必須保持開啟，但 Bot 不需要 BlueStacks 是目前作用中的視窗。螢幕可以依 Windows 電源設定關閉；電腦本身不可進入睡眠。

## 4. 設定 ADB 路徑

使用記事本開啟：

```text
D:\DinoMutantBot-App\app\config.json
```

Bot 會優先使用內建的 `app\tools\platform-tools\adb.exe`，找不到時才依序尋找目前
帳號的 Android SDK、環境變數與 BlueStacks 內附的 ADB。一般情況保持
`"executable": null` 即可；只有想改用其他 ADB 時才改成完整路徑。例如：

```json
"adb": {
  "executable": "C:\\Users\\你的Windows帳號\\AppData\\Local\\Android\\Sdk\\platform-tools\\adb.exe",
  "serial": "127.0.0.1:5555",
  "connect_on_start": true,
  "timeout": 5.0
}
```

注意：JSON 裡的 Windows 路徑要使用兩個反斜線 `\\`。存檔時請保留 UTF-8 編碼，不要新增逗號或刪除引號。

正常情況下不需要自行安裝 ADB；若 `app\tools\platform-tools\adb.exe` 遺失，請重新
解壓縮完整的 Bot 資料夾。

## 5. 第一次啟動

部署資料夾最外層只有一個日常入口：

```text
start-dashboard.cmd    網頁統計與安全控制介面
```

雙擊 `start-dashboard.cmd`。它會在目前 CMD 視窗前景執行，並開啟僅限本機使用的
`http://127.0.0.1:8780`，即使 Bot 尚未啟動也能查看歷史統計，並可從網頁
啟動純狩獵或孵蛋＋狩獵。關閉 CMD 視窗或按 `Ctrl+C` 即可停止 Dashboard；
不會建立登入啟動項或隱藏背景 watcher。關閉瀏覽器分頁不會停止 Dashboard 或 Bot。

Dashboard 永久資料位於：

```text
D:\DinoMutantBot-App\app\data\stats.sqlite3
```

它記錄本次／今日／累積狩獵數、孵化取得數、收蛋批次、親代替換、洞穴成功淘汰數、
並以每小時為一格顯示今天 24 小時內的狩獵／孵化趨勢；
Top／Mass 自動放置、驗證失敗，以及通過安全範圍檢查的最高 HP／攻擊／速度。
當天保留逐事件明細；跨日後只留下各項基本總數與每項最高屬性一筆，舊明細會自動
刪除。重新啟動或重新部署不會讓累積總數歸零，資料庫也不會隨執行時間無限膨脹。

從 Dashboard 選擇孵蛋＋狩獵後，會依序執行：孵化 → 攻擊親代 → HP 親代 → 頂尖自動放置 →
量產自動放置 → 收集所有蛋 → 洞穴數量判斷。恐龍數量大於 320 才會選擇
最弱恐龍並連續戰鬥；「最弱」使用遊戲內建功能，預設一批選取 40 隻。
戰鬥後只在「取得」畫面驗證成功時記錄該批淘汰；320 以下會直接返回。請從一般主畫面啟動，預設為
`debug + safe` 且不限制動作數，關閉視窗或按 `Ctrl+C` 即可停止。
如果這一輪孵化器沒有任何可孵化的蛋，bot 會先開啟「我的巢」、
切回「所有」並按「收集所有的蛋」，關閉巢穴後才進入 10 分鐘冷卻。
此補蛋分支不會執行攻擊／HP 親代更換、自動放置或洞穴淘汰。
可孵化蛋會排在孵化器上方，所以頂部可見區沒有「孵化」時會
直接關閉，不再滑到最底。

親代替換有固定素質防呆：HP 只接受比目前親代高 10–30、攻擊只接受高 1–3；
所有速度讀值必須介於 1–150。超出範圍會視為 OCR 或遊戲畫面異常，該次不會點擊替換。
規則位於 `config.json` 的 `hatch.stat_upgrade_guards`，日後遊戲擴大數值範圍時只需調整
該區間，不必修改程式。

完整入口若連續 15 秒找不到可執行目標，會比照狩獵地圖切換逐層回主畫面：
取消已知彈窗、關閉選擇恐龍／我的巢／孵化器；若仍是洞穴視角則用安全滑動
反向走完原本的洞穴導航路徑。
未知畫面最多嘗試兩次 Android 返回鍵。只有「主畫面錨點可見、沒有前景頁面、
背景未被遮罩變暗、洞穴不在畫面」連續兩幀成立才會重新點蛋堆；否則安全停機。
剛進遊戲若出現「自動成長結果」，整合模式會使用右側「巢穴管理快捷方式」、
關閉「我的巢」並確認回到置中主畫面；不會使用左側自動戰鬥捷徑。期間即使背景
主畫面錨點可見也不會點擊蛋堆，且只有孵化流程確認進入冷卻後才會開始狩獵。
重新登入、裝置確認或啟動獎勵若在狩獵／交接期間出現，也會讓整合模式立即失效
舊的冷卻狀態並回到孵化優先；清除前景畫面後會重新檢查孵化器。
若啟動整合模式時遊戲停在狩獵地圖或狩獵選單，會立即使用地圖退出鍵返航；
確認主畫面置中後先檢查孵化器，不會把啟動前殘留的地圖視為有效冷卻狩獵。

Dashboard 也可以啟動孵蛋＋狩獵整合模式（狀態介面 `8773`）。結合模式平時執行完整孵蛋流程；只在「無可孵化蛋」的 10 分鐘冷卻
期間切換狩獵。最後 30 秒預留給返航：完成或退出當前狩獵、將地圖
回中，並同時看到孵蛋主畫面錨點與中央蛋連續兩幀後，才重新檢查
孵化器。若返航超過 30 秒，孵蛋會延後而不會強行中斷畫面。純孵蛋測試入口
位於 `app\scripts\legacy-launchers`；日常操作與純狩獵都請使用 Dashboard。
洞穴點選會套用與狩獵相同的安全邊界：900×1600 畫面左右與上方
80 px、底部 180 px 均不會成為洞穴點擊目標。底部對話／導覽區發生
洞穴模板誤命中時，bot 會拒絕點擊並重新定位或安全復原。
洞穴判斷或戰鬥結束後也不再點底部的箭頭；bot 會依序執行
`(600,800→350,800)`、`(450,600→450,1050)` 反向滑回主畫面，
避免展開聊天列或開啟「部落」。

舊安裝中的孵蛋單步測試 CMD 會封存至
`app\scripts\legacy-launchers`，新安裝不再放入這些日常入口。若需要開發調試，
請從原始碼 `scripts` 目錄執行對應測試啟動器。

啟動器會自動檢查 Python、ADB、辨識素材與畫面擷取。全部通過才會啟動 Bot；若顯示
`FAIL`，先閱讀下方「常見問題」，不需要另外執行檢查指令。

## 6. 啟動與停止

Dashboard 的控制按鈕包括：啟動純狩獵、啟動孵蛋＋狩獵、安全停止、
重啟 Bot、重啟遊戲、ADB 擷取畫面、產生診斷包與開啟日誌資料夾。
工作流程進度會從實際規劃動作同步收蛋與洞穴導航，不必等到洞穴容量讀值才更新；
洞穴管理完成後會回到「檢查孵蛋」。
也能在沒有其他 Bot 執行時，從 Dashboard 單獨執行孵蛋一輪、攻擊親代、HP 親代、
頂尖放置、量產放置、收蛋或洞穴檢查（共用狀態介面 `8774`）。每個單階段工作都會
先把已知／未知前景安全收回置中主畫面，再導航到目標；完成或驗證失敗後同樣返家、
確認置中並自動停止。洞穴仍只在數量大於 320 時淘汰，親代仍只接受嚴格更高值。
停止、重啟與模式切換都走本機白名單接口，不接受任意 CMD 指令；同一時間只允許
一個 Bot 執行。

若要完整移除發佈包，請開啟 CMD 並執行：

```bat
start-dashboard.cmd uninstall
```

輸入 `DELETE` 確認後，卸載器會驗證本機服務身份、安全停止 Bot 與 Dashboard、
關閉本包啟動的 ADB server，再從 Windows 暫存目錄刪除整個發佈資料夾。
仍被 Windows 鎖定的檔案會排程在下次重新開機時刪除，不需要使用工作管理員。

Dashboard 可查詢狩獵與失敗次數、啟動、安全停止、重啟及執行診斷。

- `fast`：一般點擊與選恐龍 300 ms、狩獵 900 ms、確認 1200 ms、無目標掃描 250 ms。
- `safe`：一般點擊與選恐龍 1500 ms、狩獵 5000 ms、確認 3000 ms、無目標掃描 500 ms。

這些操作時間是「最長等待期限」，不是每次都固定停住。Bot 會持續檢查下一個 UI：
快電腦偵測到後立即繼續，慢電腦則等到期限；即使單次擷取太慢超過期限，也至少完成
`verify.minimum_checks` 次檢查再判定失敗。

進階使用者也能直接執行 runner 覆寫時間（單位為毫秒）：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass `
  -File D:\DinoMutantBot-App\app\scripts\run-windows.ps1 `
  -Mode runtime -Speed fast `
  -DinosaurDelayMs 800 -HuntButtonDelayMs 2500 `
  -HuntConfirmDelayMs 1800 -IdleDelayMs 150
```

套用新參數時，控制視窗會自動停止並重啟 LOG 視窗。正常情況會依序看到：

```text
Detect
Planning
Action
Verify | Success
```

在控制視窗按 `Q` 即可停止 Bot。完整日誌會保存在：

```text
D:\DinoMutantBot-App\app\logs\YYYYMMDD.log
```

### 產生診斷包

Bot 沒有正常運作時，在控制視窗按 `E`。系統會產生 ZIP 並自動開啟資料夾：

```text
D:\DinoMutantBot-App\app\diagnostics
```

將最新的 `dino-diagnostic-*.zip` 上傳給 Codex，即可請它分析故障原因、建議的處理步驟，
以及哪些問題需要由維護者修改程式。預設診斷包不含遊戲畫面；若需要分享畫面，請進入
`D` 診斷工具並選擇 `7`，畫面會明確提醒後才擷取。

診斷包會遮蔽常見 Token、密碼、電子郵件與使用者路徑，而且不會開啟遠端連線或提供
電腦控制權。上傳前仍可自行解壓縮確認內容。

## 7. AI 與外部工具接口

Bot 啟動後，同一台電腦上的 AI 或工具可直接讀取：

```text
完整狀態  http://127.0.0.1:8765/status
最近動作  http://127.0.0.1:8765/actions
目前參數  http://127.0.0.1:8765/settings
健康檢查  http://127.0.0.1:8765/health
安全停止  POST http://127.0.0.1:8765/control/stop
重啟遊戲  POST http://127.0.0.1:8765/control/restart-game
```

這些接口只監聽本機。狀態接口為唯讀，HTTP 控制只允許安全停止及重啟固定的 Dino Mutant
App；AI 無法直接點擊遊戲、指定其他 App 或送出任意 ADB 指令。Port 可在控制視窗按 `P`
更改。`/status` 會回傳成功狩獵、
信箱循環、操作總數、驗證失敗、黑屏、遊戲重啟、目前階段與最近操作。

若選定的 Port 已被占用，直接按 Enter 或輸入 `N` 會改用下一個 Port。只有程序命令列、
`/health` API 身分與 PID 都確認為 Dino Bot 時才會出現 `K` 清理選項；輸入確認碼後
系統還會再檢查一次占用者，避免誤關剛好接手 Port 的其他程序。清理必須依畫面輸入
`CLEAN-<Port>`，若需強制結束還要再輸入 `FORCE-<PID>`。未知程式不會出現清理
選項，也不會被控制台關閉。

資料夾內附 Codex Skill。使用 Codex 開啟這個資料夾後可直接輸入：

```text
$control-dino-bot 幫我查狩獵進度
$control-dino-bot 用 8877 Port 查詢狀態
$control-dino-bot 請重新啟動 Bot
$control-dino-bot 請重新啟動 Dino Mutant App
```

Skill 只會使用固定控制腳本；啟動、停止、重啟 Bot 與重啟遊戲 App 必須由使用者當次
明確要求，不會自行掃描 Port、執行任意 ADB 指令或探索遊戲畫面。控制腳本會先驗證
`/health`，再交叉確認 API PID、Port 占用者與 Bot 命令列；驗證失敗便不送出控制請求。

## 8. Bot 目前會做什麼

- 辨識安全的恐龍，避開我方藍色路徑與底部 UI。
- 選擇最大群組、按狩獵、按確認，並重新驗證結果。
- 每 10 次重置地圖，讓中央蛋巢回到畫面中心。
- 每 30 次開信箱，依序「全部獲取 → 資源獲取 → 關閉」。
- 隊伍滿額或目標太強時，暫停 5 分鐘後再試。
- 遊戲畫面持續全黑超過 45 秒時，自動重啟遊戲 App；短暫轉場不會重啟。
- 若因其他裝置登入而出現「不同設備的歷史記錄」，會自動選擇此設備並繼續。
- 遊戲重啟後若出現「自動成長結果」，會經由自動戰鬥快捷視窗返回採集地圖。

> 自動選擇此設備代表目前模擬器會接手遊戲連線；若你仍要在另一台裝置操作，請先停止 Bot。

## 9. 常見問題

### 顯示 `ADB executable not found`

先確認 `app\tools\platform-tools\adb.exe` 存在；若不存在，請重新解壓縮完整的
Bot 資料夾。若 `config.json` 的 `adb.executable` 有填路徑，請確認該位置真的有
`adb.exe`，或改回 `null` 使用內建 ADB。

### 顯示找不到 ADB 裝置

確認 BlueStacks 已開啟 ADB，並重新啟動 BlueStacks 後再執行環境檢查。

### Bot 一直顯示 `Planning | no actionable target`

這通常不是卡住。Bot 可能在等待安全目標，避免點到我方藍色路徑上的恐龍。
`v0.1.1` 起，即使畫面只辨識到我方藍色路徑，持續多幀後也會自行離開並
重新置中地圖，不會無限掃描同一畫面。

### 在其他裝置登入後，Bot 沒有切回模擬器

確認 BlueStacks 是直向 `900 × 1600`，並讓 Dino Mutant 保持在畫面最上層。新版 Bot
會以 `0.88、0.90、0.95、1.00` 等 UI 尺寸辨識「不同設備的歷史記錄」確認按鈕；
若遊戲更新後外觀大幅改變，仍可能需要更新 `device-history-confirm-button.png` 或
manifest 的 `scales`。

### 重啟後停在「自動成長結果」

新版 Bot 會先開啟「自動戰鬥捷徑」，再點擊快捷視窗外側返回採集地圖。若畫面仍停住，
請保留當時的 `900 × 1600` 截圖與日誌，避免手動改變 UI 後無法重現。

### Bot 點錯或完全辨識不到恐龍

先確認解析度是 `900 × 1600`，並且遊戲 UI 縮放、語言與分享來源相近。不同解析度、DPI 或大幅更新後，可能需要重新製作辨識素材。

### BlueStacks 視窗短暫全黑

先等待約一分鐘。若 ADB 遊戲畫面也持續全黑，Bot 會自動重啟 Dino Mutant App。
`v0.1.1` 起，全黑畫面不會進入目標辨識或點擊驗證，也會明確記錄重啟冷卻剩餘時間。
若每次按下「狩獵」都持續全黑，這是 BlueStacks/Unity 渲染異常，建議完整重啟
BlueStacks，並嘗試切換 BlueStacks 的圖形渲染器。

## 10. 分享給其他人前

請壓縮整個：

```text
D:\DinoMutantBot-App
```

可先刪除或清空下列資料夾，讓壓縮檔較小且不包含舊日誌：

```text
D:\DinoMutantBot-App\app\logs
D:\DinoMutantBot-App\app\debug
D:\DinoMutantBot-App\app\capture
```

接收者解壓後只需要設定 BlueStacks ADB、修改 `config.json` 的 ADB 路徑，再雙擊
`start-dashboard.cmd`；首次啟動會自動安裝 Windows runtime。
